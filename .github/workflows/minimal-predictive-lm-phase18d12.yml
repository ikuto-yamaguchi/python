name: minimal-predictive-lm-phase18d12

on:
  pull_request:
    paths:
      - "minimal-predictive-lm/data/phase18d12_multi_primitive_stream.json"
      - "minimal-predictive-lm/src/minimal_predictive_lm/phase18d10_primitive_core.py"
      - "minimal-predictive-lm/src/minimal_predictive_lm/phase18d11_ungrouped_online_residual.py"
      - "minimal-predictive-lm/src/minimal_predictive_lm/phase18d12_multi_primitive_core.py"
      - "minimal-predictive-lm/src/minimal_predictive_lm/phase18d12_multiple_ungrouped_primitives.py"
      - "minimal-predictive-lm/tests/test_phase18d12_multiple_ungrouped_primitives.py"
      - "minimal-predictive-lm/docs/phase18d12_multiple_ungrouped_primitives.md"
      - ".github/workflows/minimal-predictive-lm-phase18d12.yml"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  phase18d12:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: minimal-predictive-lm/pyproject.toml
      - name: Install
        run: python -m pip install -e minimal-predictive-lm pytest
      - name: Multiple primitive theorem tests
        id: tests
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          mkdir -p minimal-predictive-lm/results
          python -m pytest -q minimal-predictive-lm/tests/test_phase18d12_multiple_ungrouped_primitives.py 2>&1 | tee minimal-predictive-lm/results/phase18d12-test.log
      - name: Run Phase 18d-12 experiment
        id: experiment
        continue-on-error: true
        working-directory: minimal-predictive-lm
        shell: bash
        run: |
          set -o pipefail
          python -m minimal_predictive_lm.phase18d12_multiple_ungrouped_primitives 2>&1 | tee results/phase18d12-run.log
      - name: Upload Phase 18d-12 diagnostics and results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phase18d12-results
          retention-days: 7
          if-no-files-found: error
          path: |
            minimal-predictive-lm/results/phase18d12.json
            minimal-predictive-lm/results/phase18d12.md
            minimal-predictive-lm/results/phase18d12-test.log
            minimal-predictive-lm/results/phase18d12-run.log
      - name: Enforce Phase 18d-12 success
        if: always()
        run: |
          test "${{ steps.tests.outcome }}" = "success"
          test "${{ steps.experiment.outcome }}" = "success"
