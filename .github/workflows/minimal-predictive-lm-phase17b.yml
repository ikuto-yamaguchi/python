name: minimal-predictive-lm-phase17b

on:
  pull_request:
    paths:
      - "minimal-predictive-lm/src/minimal_predictive_lm/phase17a_*.py"
      - "minimal-predictive-lm/src/minimal_predictive_lm/phase17b_*.py"
      - "minimal-predictive-lm/tests/test_phase17b_*.py"
      - "minimal-predictive-lm/docs/phase17_*.md"
      - ".github/workflows/minimal-predictive-lm-phase17b.yml"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  phase17b:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: minimal-predictive-lm/pyproject.toml
      - name: Install
        run: python -m pip install -e minimal-predictive-lm
      - name: Selective scaling regression tests
        shell: bash
        run: |
          set -o pipefail
          mkdir -p minimal-predictive-lm/results
          python -m unittest discover -s minimal-predictive-lm/tests -p 'test_phase17b_partial_resource_scaling.py' -v 2>&1 | tee minimal-predictive-lm/results/phase17b-test.log
      - name: Run selective fixed-budget pilot
        working-directory: minimal-predictive-lm
        run: python -m minimal_predictive_lm.phase17b_partial_resource_scaling
      - name: Upload Phase 17b diagnostics and results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phase17b-results
          retention-days: 7
          if-no-files-found: warn
          path: |
            minimal-predictive-lm/results/phase17b-test.log
            minimal-predictive-lm/results/phase17b.json
            minimal-predictive-lm/results/phase17b.md
