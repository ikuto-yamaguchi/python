name: minimal-predictive-lm-phase18a1

on:
  pull_request:
    paths:
      - "minimal-predictive-lm/src/minimal_predictive_lm/phase17f_*.py"
      - "minimal-predictive-lm/src/minimal_predictive_lm/phase18a1_*.py"
      - "minimal-predictive-lm/tests/test_phase18a1_*.py"
      - "minimal-predictive-lm/docs/phase18a1_*.md"
      - ".github/workflows/minimal-predictive-lm-phase18a1.yml"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  phase18a1:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: minimal-predictive-lm/pyproject.toml
      - name: Install
        run: python -m pip install -e minimal-predictive-lm
      - name: Noisy Japanese grounding theorem tests
        shell: bash
        run: |
          set -o pipefail
          mkdir -p minimal-predictive-lm/results
          python -m unittest discover -s minimal-predictive-lm/tests -p 'test_phase18a1_noisy_japanese_grounding.py' -v 2>&1 | tee minimal-predictive-lm/results/phase18a1-test.log
      - name: Run Phase 18a-1 experiment
        working-directory: minimal-predictive-lm
        run: python -m minimal_predictive_lm.phase18a1_noisy_japanese_grounding
      - name: Upload Phase 18a-1 diagnostics and results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phase18a1-results
          retention-days: 7
          if-no-files-found: warn
          path: |
            minimal-predictive-lm/results/phase18a1-test.log
            minimal-predictive-lm/results/phase18a1.json
            minimal-predictive-lm/results/phase18a1.md
