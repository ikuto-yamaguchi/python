name: minimal-predictive-lm-phase18a7

on:
  pull_request:
    paths:
      - "minimal-predictive-lm/src/minimal_predictive_lm/phase18a7_*.py"
      - "minimal-predictive-lm/tests/test_phase18a7_*.py"
      - "minimal-predictive-lm/docs/phase18a7_*.md"
      - ".github/workflows/minimal-predictive-lm-phase18a7.yml"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  phase18a7:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: minimal-predictive-lm/pyproject.toml
      - name: Install
        run: python -m pip install -e minimal-predictive-lm
      - name: Recursive discourse theorem tests
        shell: bash
        run: |
          set -o pipefail
          mkdir -p minimal-predictive-lm/results
          python -m unittest discover -s minimal-predictive-lm/tests -p 'test_phase18a7_recursive_conditional_discourse.py' -v 2>&1 | tee minimal-predictive-lm/results/phase18a7-test.log
      - name: Run Phase 18a-7 experiment
        working-directory: minimal-predictive-lm
        run: python -m minimal_predictive_lm.phase18a7_recursive_conditional_discourse
      - name: Upload Phase 18a-7 diagnostics and results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phase18a7-results
          retention-days: 7
          if-no-files-found: warn
          path: |
            minimal-predictive-lm/results/phase18a7-test.log
            minimal-predictive-lm/results/phase18a7.json
            minimal-predictive-lm/results/phase18a7.md
