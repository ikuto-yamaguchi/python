# Reproduce Cycle 003

```bash
python -m pip install numpy
python research/intelligence_swarm/tracks/A_semantic_identity/consequence_anchored_unit_cycle003.py > /tmp/a_cycle003.json
```

Python 3.11、NumPyのみ。seedは1, 7, 19で固定。出力JSONの`mean`、`paired_held_joint_gap`、`resources`を報告値として使用する。
