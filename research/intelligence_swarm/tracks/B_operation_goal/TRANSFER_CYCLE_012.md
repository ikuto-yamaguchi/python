# Cycle 012 Transfer Notes

## Aへ
匿名変換応答の集合一致では不十分。各変換とraw表現成分のlinkが独立取得集合で再生成され、Rename時にも再構築される必要がある。

## Cへ
State-axis / Target-link / Argument-link / Direction / Goal shuffleを個別に行い、正しいlinkだけを壊した際に対応能力だけが崩れることを要求する。

## Dへ
取得時same-unique operation unitは0。memory eligible unitは0であり、保存・干渉・忘却研究は再開しない。

## Eへ
`anonymous commutator multiset defines operation identity`は不採用下位仮説候補。oracle軸を外しただけでidentity birthと認定しない。
